322 Coin Change \
https://leetcode.com/problems/coin-change/description/

## Статус: Изучена

## Условие:

Дан массив с денежными значениями монет и определенная сумма. Необходимо
набрать сумму наименьшим количеством монет, то есть найти наименьшее количество монет, чтобы собрать сумму.

## Подход: 

Динамическое программирование (Dynamic Programming);
Нахождение оптимального минимального варианта.

время: O (amount * coin.size); Перебор всех значений от 1 до amount O (amount) и вложенный цикл проходящий через все монеты O (coin.size) \
память: O (amount); Хранение линейного массива размера (amount + 1) в памяти
 
## Пример:

```
coins = [1, 2, 5], amount = 3
dp = [0, 8, 8, 8] //Все значения, кроме первого, равны amount + 1, если конечное значение
останется равным amount + 1, то вернётся -1.

Перебор от 1 до amount, включительно

1. i = 1 (сумма)
    |
[0, 8, 8, 8]

Перебор всех монет:
1: if (i - coin) >= 0: dp[i] = min(dp[i], dp[i - coin] + 1) // dp[i] = min(8, dp[1 - 1] + 1) = min(8, 0 + 1) = 1
//dp = [0, 1, 8, 8]
2: if (i - coin) >= 0: // 1 - 2 < 0 FALSE
//dp = [0, 1, 8, 8]
3: if (i - coin) >= 0: // 1 - 5 < 0 FALSE
//ap = [0, 1, 8, 8]

2. i = 2 (сумма)
       |
[0, 1, 8, 8]

Перебор всех монет:
1: if (i - coin) >= 0: dp[i] = min(dp[i], dp[i - coin] + 1) // dp[i] = min(8, dp[2 - 1] + 1) = min(8, 1 + 1) = 2
//dp = [0, 1, 2, 8]
2: if (i - coin) >= 0: dp[i] = min(dp[i], dp[i - coin] + 1) // dp[i] = min(8, dp[2 - 2] + 1) = min(8, 0 + 1) = 1
//dp = [0, 1, 1, 8]
3: if (i - coin) >= 0: // 2 - 5 < 0 FALSE
//ap = [0, 1, 1, 8]

3. i = 2 (сумма)
       |
[0, 1, 8, 8]

Перебор всех монет:
1: if (i - coin) >= 0: dp[i] = min(dp[i], dp[i - coin] + 1) // dp[i] = min(8, dp[3 - 1] + 1) = min(8, 2 + 1) = 3
//dp = [0, 1, 2, 3]
2: if (i - coin) >= 0: dp[i] = min(dp[i], dp[i - coin] + 1) // dp[i] = min(8, dp[3 - 2] + 1) = min(8, 1 + 1) = 2
//dp = [0, 1, 1, 2]
3: if (i - coin) >= 0: // 3 - 5 < 0 FALSE
//dp = [0, 1, 1, 2]

Результат: dp[amount] = 2
```

## Краевые случаи:

При amount = 0 код вернёт значение dp[0] = 0.

## Код:

```c++
class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        vector<int> dp(amount + 1, amount + 1);
        dp[0] = 0;

        for (int i{1}; i <= amount; i++) {
            for (const auto& coin : coins) {
                if (i - coin >= 0) {
                    dp[i] = std::min(dp[i], dp[i - coin] + 1);
                }
            }
        }

        if (dp[amount] != amount + 1) {
            return dp[amount];
        }else {
            return -1;
        }
    }
};
```
